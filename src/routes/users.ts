import { Router } from "express";

const router = Router();

router.get("/:userId", (req, res, next) => {
  // ALL: Get user information, including processed list of classes
});

export default router;
